import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonContainer: {
    alignItems: "center",
    justifyContent: "center",
    padding: 24,
  },
  container: {
    alignItems: "center",
    marginTop: 20,
  },
  scrollView: {
    height: "58%",
  },
  calculatedValue: {
    marginTop: 5,
    fontSize: 16,
    textAlign: "center",
  },
});
